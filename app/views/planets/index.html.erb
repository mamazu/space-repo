<% content_for :javascript do %>
	<script type="text/javascript" src="<%= asset_path('custom.js') %>"> </script>
<% end %>

<% @planets.each do |planet| %>
	<h1>Planet: <%= planet.name %></h1>
	<div class="mui-btn" onclick="boo(undefined, undefined)">
		Edit
	</div>
		<div>
			<%= form_with model: planet, class: 'mui-form--inline' do |f| %>
				<div class="mui-textfield input">
					<%= f.text_field :name %>
				</div>
				<%= f.submit "Rename", :class => 'mui-btn' %>
			<% end %>
		</div>
	<h3>Buildings</h3>

	<% planet.buildings.each do |b| %>
		<div class="mui-panel">
				<b><%= b.name %></b>
				<div class="mui-divider"></div>
				Level <%= b.level %>
		</div>
	<% end %>
<% end %>